# The sample configuration for korp.pluginlib is in config.py.template,
# stored in the repository. It should be copied (or symlinked) to
# config.py, to be modified locally and not not be stored in the
# repository.

config.py
